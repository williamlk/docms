<?xml version="1.0" encoding="UTF-8" ?>    
<!DOCTYPE mapper    
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.shyfb.docms.dao.UserDao">

	<sql id="columns">
		u.id,
		u.name,
		u.password,
		u.email,
		u.phone,
		u.status,
		u.created_time,
		u.updated_time,
		u.del
	</sql>
	<insert id="insert" parameterType="org.shyfb.docms.entity.User">
		insert into tb_user (id,
		name,password,email,phone,status,del)
		values (#{id,jdbcType=INTEGER},
		#{name,jdbcType=VARCHAR},
		#{password,jdbcType=VARCHAR},
		#{email,jdbcType=VARCHAR},
		#{phone,jdbcType=VARCHAR},
		#{status,jdbcType=INTEGER},
		#{del,jdbcType=INTEGER})
	</insert>

	<select id="queryById" parameterType="java.lang.Integer"
		resultType="org.shyfb.docms.entity.User">
		select
		<include refid="columns" />
		from tb_user u
		where id=#{id,jdbcType=INTEGER}
	</select>

	<select id="query" parameterType="java.util.Map" resultType="org.shyfb.docms.entity.User">
		select
		<include refid="columns" />
		from tb_user u
		where 1=1
		<if test="name != null and name != ''">
			and name=#{name,jdbcType=VARCHAR}
		</if>
		<if test="email != null and email != ''">
			and email=#{email,jdbcType=VARCHAR}
		</if>
		<if test="phone != null and phone !=''">
			and phone=#{phone,jdbcType=VARCHAR}
		</if>
	</select>

	<update id="update" parameterType="org.shyfb.docms.entity.User">
		update tb_user
		<set>
			<if test="name != null">
				name = #{name,jdbcType=VARCHAR},
			</if>
			<if test="password != null and password !=''">
				password = #{password,jdbcType=VARCHAR},
			</if>
			<if test="email != null and email !=''">
				email = #{email,jdbcType=VARCHAR},
			</if>
			<if test="phone != null and phone !=''">
				phone = #{phone,jdbcType=VARCHAR},
			</if>
			<if test="status != null">
				status = #{status,jdbcType=INTEGER},
			</if>
			<if test="del != null">
				del = #{del,jdbcType=INTEGER},
			</if>
		</set>
		where id = #{id,jdbcType=INTEGER}
	</update>


</mapper>